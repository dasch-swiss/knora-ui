dist: trusty
sudo: required

language: node_js

node_js:
  - "8"

cache:
  directories:
    - "node_modules"

addons:
  chrome: stable

before_script:
  - npm install

script:
  - npm ng t --project @knora/core --watch=false --browsers=ChromeHeadless
