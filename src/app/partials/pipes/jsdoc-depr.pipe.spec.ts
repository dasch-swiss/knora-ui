import { JsdocDeprPipe } from './jsdoc-depr.pipe';

describe('JsdocDeprPipe', () => {
  it('create an instance', () => {
    const pipe = new JsdocDeprPipe();
    expect(pipe).toBeTruthy();
  });
});
