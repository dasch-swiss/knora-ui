import { TrimBracketsPipe } from './trim-brackets.pipe';

describe('TrimBracketsPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimBracketsPipe();
    expect(pipe).toBeTruthy();
  });
});
